<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".CopyToClipActivity">

    <EditText
        android:id="@+id/copyMessageEditText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:ems="10"
        android:inputType="textPersonName"
        android:hint="@string/enter_message"
        android:layout_marginTop="40dp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <ImageButton
        android:id="@+id/backToMain2Button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintEnd_toEndOf="@+id/copyMessageEditText"
        app:srcCompat="@drawable/ic_back"
        app:layout_constraintTop_toBottomOf="@id/copyMessageEditText"
        android:layout_marginTop="@dimen/margin_top"
        android:layout_marginStart="@dimen/margin_top"
        android:background="@drawable/rectangle"/>

    <ImageButton
        android:id="@+id/copyButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/margin_top"
        android:background="@drawable/rectangle"
        app:layout_constraintEnd_toStartOf="@id/backToMain2Button"
        android:layout_marginEnd="@dimen/margin_top"
        app:layout_constraintTop_toBottomOf="@id/copyMessageEditText"
        app:srcCompat="@drawable/ic_baseline_content_copy_24" />
</androidx.constraintlayout.widget.ConstraintLayout>